<?php

/**
 * zxf/captcha 滑动验证码配置文件
 *
 * 本配置文件包含滑动验证码的所有可配置项，每个配置项都配有详细的中文注释
 * 说明其用途、使用场景和推荐配置。
 *
 * @package     zxf\Captcha
 * @author      zhaoxianfang <zhaoxianfang@163.com>
 * @license     MIT
 */

return [
    /*
    |--------------------------------------------------------------------------
    | 前端图标组图片路径
    |--------------------------------------------------------------------------
    |
    | 该图片包含验证码界面所需的各种图标（如关闭按钮、刷新按钮等）
    | 图片格式要求：PNG 格式，透明背景
    | 推荐使用包内默认提供的图标组
    |
    | 使用场景：验证码弹出框的工具栏图标显示
    | 自定义方法：替换 resources/assets/images/icon.png 文件
    |
    | 注意：如果配置的路径不存在，系统会自动使用包内默认路径
    |
    */
    'tool_icon_img' => '',

    /*
    |--------------------------------------------------------------------------
    | 黑色滑块图片路径
    |--------------------------------------------------------------------------
    |
    | 该图片是用户拖动的滑块主体图片，为黑色缺口形状
    | 图片格式要求：PNG 格式，透明背景
    | 图片尺寸要求：50x50 像素
    |
    | 使用场景：用户在滑动条上拖动的滑块元素
    | 自定义方法：替换 resources/assets/images/mark_02.png 文件
    |
    | 注意：如果配置的路径不存在，系统会自动使用包内默认路径
    |
    */
    'slide_dark_img' => '',

    /*
    |--------------------------------------------------------------------------
    | 透明滑块图片路径（背景缺口）
    |--------------------------------------------------------------------------
    |
    | 该图片用于在背景图上创建缺口效果，需要与滑块图片形状匹配
    | 图片格式要求：PNG 格式，透明背景，白色缺口区域
    | 图片尺寸要求：50x50 像素
    |
    | 使用场景：背景图片上的缺口显示
    | 自定义方法：替换 resources/assets/images/mark_01.png 文件
    | 注意：必须与 slide_dark_img 形状完全匹配，否则验证会出问题
    |
    | 注意：如果配置的路径不存在，系统会自动使用包内默认路径
    |
    */
    'slide_transparent_img' => '',

    /*
    |--------------------------------------------------------------------------
    | 背景图片目录
    |--------------------------------------------------------------------------
    |
    | 验证码将从此目录随机选择背景图片
    | 支持的图片格式：png, jpg, jpeg, gif, webp
    | 推荐图片尺寸：240x150 像素（与 bg_width、bg_height 配置一致）
    |
    | 使用场景：验证码背景图片的来源目录
    | 自定义方法：添加或替换 resources/assets/images/bg/ 目录下的图片
    | 提示：建议准备多张背景图，增强验证码的随机性和安全性
    |
    | 注意：如果配置的路径不存在，系统会自动使用包内默认路径
    |
    */
    'bg_images_dir' => '',

    /*
    |--------------------------------------------------------------------------
    | 指定背景图片列表
    |--------------------------------------------------------------------------
    |
    | 如果配置了具体的背景图片路径列表，将优先使用此配置
    | 留空数组 [] 则表示自动扫描 bg_images_dir 目录
    |
    | 使用场景：需要精确控制可用的背景图片时
    | 示例配置：
    | 'bg_images' => [
    |     '/path/to/bg1.png',
    |     '/path/to/bg2.jpg',
    | ],
    |
    */
    'bg_images' => [],

    /*
    |--------------------------------------------------------------------------
    | 容错像素值
    |--------------------------------------------------------------------------
    |
    | 该值决定用户滑动位置的容错范围，单位为像素
    | - 值越大：用户体验越好，滑动越容易通过，但安全性降低
    | - 值越小：安全性越高，但可能增加用户操作难度
    |
    | 推荐值：
    | - 高安全性场景（如支付、登录）：1-2
    | - 普通场景（如评论、投票）：3-5
    | - 宽松场景（如内部分页验证）：5-8
    |
    | 默认值：3
    |
    */
    'fault_tolerance' => 3,

    /*
    |--------------------------------------------------------------------------
    | 最大错误次数
    |--------------------------------------------------------------------------
    |
    | 用户验证失败次数超过此值将强制刷新验证码
    | 这是为了防止暴力破解尝试
    |
    | 推荐值：
    | - 高安全场景：3-5
    | - 普通场景：10
    | - 宽松场景：20
    |
    | 默认值：10
    |
    */
    'max_error_count' => 10,

    /*
    |--------------------------------------------------------------------------
    | 背景图片宽度
    |--------------------------------------------------------------------------
    |
    | 验证码图片的宽度，单位为像素
    | 修改此值需要同步修改前端 CSS 样式
    |
    | 默认值：240
    | 推荐值：240-320（过小影响识别，过大影响加载速度）
    |
    */
    'bg_width' => 240,

    /*
    |--------------------------------------------------------------------------
    | 背景图片高度
    |--------------------------------------------------------------------------
    |
    | 验证码图片的高度，单位为像素
    | 修改此值需要同步修改前端 CSS 样式
    |
    | 默认值：150
    | 推荐值：150-200（过小影响操作，过大影响页面布局）
    |
    */
    'bg_height' => 150,

    /*
    |--------------------------------------------------------------------------
    | 滑块宽度
    |--------------------------------------------------------------------------
    |
    | 滑动验证码缺口的宽度，单位为像素
    | 修改此值需要同步准备匹配的滑块图片
    |
    | 默认值：50
    | 推荐值：40-60（过小难以操作，过大降低安全性）
    |
    */
    'mark_width' => 50,

    /*
    |--------------------------------------------------------------------------
    | 滑块高度
    |--------------------------------------------------------------------------
    |
    | 滑动验证码缺口的高度，单位为像素
    | 修改此值需要同步准备匹配的滑块图片
    |
    | 默认值：50
    | 推荐值：40-60
    |
    */
    'mark_height' => 50,

    /*
    |--------------------------------------------------------------------------
    | 图片输出格式
    |--------------------------------------------------------------------------
    |
    | 验证码图片的输出格式
    | 可选值：'webp' 或 'png'
    |
    | WebP 格式优点：
    | - 文件体积小（比 PNG 小 30-50%）
    | - 加载速度快
    | - 需要浏览器支持 WebP
    |
    | PNG 格式优点：
    | - 兼容性最好
    | - 所有浏览器都支持
    |
    | 推荐值：'webp'（现代浏览器都支持）
    |
    */
    'output_format' => 'webp',

    /*
    |--------------------------------------------------------------------------
    | WebP 图片质量
    |--------------------------------------------------------------------------
    |
    | 当 output_format 为 'webp' 时的图片质量
    | 取值范围：0-100
    |
    | - 值越大：图片质量越好，文件越大
    | - 值越小：图片质量越差，文件越小
    |
    | 推荐值：40-60（平衡质量和体积）
    |
    */
    'webp_quality' => 40,

    /*
    |--------------------------------------------------------------------------
    | PNG 图片压缩级别
    |--------------------------------------------------------------------------
    |
    | 当 output_format 为 'png' 时的压缩级别
    | 取值范围：0-9
    |
    | - 0：无压缩，文件最大，处理最快
    | - 9：最大压缩，文件最小，处理最慢
    |
    | 推荐值：6-7（平衡压缩率和处理速度）
    |
    */
    'png_quality' => 7,

    /*
    |--------------------------------------------------------------------------
    | Session 前缀
    |--------------------------------------------------------------------------
    |
    | 验证码存储在 Session 中的键名前缀
    | 修改此值可避免与其他验证码系统冲突
    |
    | 注意：修改后需要清除浏览器 Session，否则验证会失败
    |
    */
    'session_prefix' => 'xf_captcha',

    /*
    |--------------------------------------------------------------------------
    | 资源路由配置
    |--------------------------------------------------------------------------
    |
    | 用于配置验证码静态资源的路由前缀
    | 当使用框架集成的资源路由时使用
    |
    | Laravel 使用示例：
    | - 访问 /captcha/js 获取 JS 文件
    | - 访问 /captcha/css 获取 CSS 文件
    | - 访问 /captcha/image 获取验证码图片
    |
    */
    'route_prefix' => 'captcha',

    /*
    |--------------------------------------------------------------------------
    | 路由中间件
    |--------------------------------------------------------------------------
    |
    | 验证码路由使用的中间件
    | 在 Laravel 中生效
    |
    | 推荐配置：
    | - 生产环境：['web'] 或 ['api', 'throttle:60,1']
    | - 开发环境：[]（空数组，无限制）
    |
    */
    'route_middleware' => ['web'],

    /*
    |--------------------------------------------------------------------------
    | 前端配置
    |--------------------------------------------------------------------------
    |
    | 前端 JavaScript 配置选项
    | 这些配置将传递给前端 xfCaptcha 对象
    |
    */
    'frontend' => [
        // 触发验证码的 DOM 选择器
        'handle_dom' => '.xf-captcha',

        // 获取验证码图片的 URL
        // 留空则使用自动生成的路由
        'image_url' => '',

        // 验证验证码的 URL
        // 留空则使用自动生成的路由
        'check_url' => '',

        // 提示文字
        'placeholder' => '点击按钮进行验证',

        // 滑动提示文字
        'slide_text' => '拖动左边滑块完成上方拼图',

        // 验证成功提示
        'success_text' => '✓ 验证成功',

        // 验证失败提示
        'fail_text' => '验证失败，请重试',

        // 是否显示关闭按钮
        'show_close' => true,

        // 是否显示刷新按钮
        'show_refresh' => true,

        // 是否显示水波纹效果
        'show_ripple' => true,
    ],
];
